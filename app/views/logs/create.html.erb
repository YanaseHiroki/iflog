<%= render "shared/header" %>
<main>
  <div class="main">
    <div class="title-container">
      <h1 class="title">今日もおつかれさまでした！</h1>
    </div>
    <div class="image">
    <%# Userテーブルに画像のカラムを追加してここでuploadして表示 %>
    <% if @user.image.attached? %>
      <%= image_tag @user.image %>
    <% else %>
      <%= image_tag 'latte-art.jpg' %>
    <% end %>
    </div>
    <div class="message">
      <% if @days > 0 %>
        <%= "今日で#{@days + 1}日連続です！" %><br>
        この調子で続けていきましょう！
      <% elsif @days < 0 %>
        <%= "今日は#{-@days}日ぶりです！" %><br>
        これから続けていきましょう！
      <% else %>
        1日目です！<br>
        これから続けていきましょう！
      <% end %>
    </div>
    <div class="summary">
      <%= "１日#{@plan.target}回、#{@plan.trigger}#{@plan.habit}" %>
    </div>

    <%= render "shared/table" %>
    <%= link_to "記録を追加する", new_plan_log_path(@plan.id), method: :get, class: "hyper-link" %>
    <%= link_to "達成率を振り返る", plan_path(@plan.id), method: :get, class: "hyper-link" %><br>
  </div>
</main>